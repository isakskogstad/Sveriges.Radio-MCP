# Sveriges Radio MCP Server Configuration

# Server Port (default: 3000)
PORT=3000

# Optional: Bearer Token Authentication
# If set, all MCP endpoint requests must include:
# Authorization: Bearer YOUR_TOKEN_HERE
# Leave empty to disable authentication (public access)
MCP_AUTH_TOKEN=

# Example with token enabled:
# MCP_AUTH_TOKEN=my-secret-token-12345

# Usage with curl:
# curl -H "Authorization: Bearer my-secret-token-12345" https://your-server.com/sse
